#include "featureC.h"

#test featureCtest
        fail_unless(featureC(1,3) == 3, "trivial case");
	fail_unless(featureC(3,1) == 3, "trivial case");
	fail_unless(featureC(8,8) == 8, "trivial case");
	fail_unless(featureC(1,3) == 1, "fail case");

#main-pre
	srunner_set_xml(sr, "report_featureC.xml");
